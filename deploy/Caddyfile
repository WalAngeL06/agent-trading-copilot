# HTTPS for DOMAIN via Let's Encrypt (ports 80 and 443 must reach this server).
{$DOMAIN} {
	encode zstd gzip

	@backend path /api/* /health/*
	handle @backend {
		reverse_proxy app:8000
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
